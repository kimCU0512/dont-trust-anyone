# 「아무도 믿지마」 페이지별 기능명세서 v1.1

- 플랫폼: 웹 (완전 정적 배포 — Vercel / Netlify / GitHub Pages)
- 스택: React + TypeScript, 로컬 state(필요 시 Zustand), 스토리 데이터 JSON
- 서버/DB 없음, 게임 실행 중 외부 API 호출 없음

---

## 0. 화면(페이지) 구성 개요

| 화면 ID | 화면명 | 라우팅 | 비고 |
|---|---|---|---|
| P-00 | 타이틀 화면 | `/` (초기 진입) | 게임 시작점 |
| P-01 | 인트로 (입장 연출) | 내부 상태 전환 | "문이 잠긴다" 연출 |
| P-10 | 스테이지 화면 | 내부 상태 전환 | 스테이지 1~5 공통 템플릿 |
| P-20 | 리셋(게임오버) 화면 | 내부 상태 전환 | 하트 0 시 |
| P-30 | 진엔딩 화면 | 내부 상태 전환 | 열쇠조각 5개 |
| P-31 | 배드엔딩 화면 | 내부 상태 전환 | 열쇠조각 5개 미만 |

> SPA 단일 페이지 내에서 `gamePhase` 상태값(`title | intro | stage | reset | endingTrue | endingBad`)으로 화면을 전환하는 구조를 권장. URL 라우팅을 나누면 새로고침/직접 접근 시 상태가 깨지므로 내부 상태 전환이 안전함.

---

## 1. P-00 타이틀 화면

**목적**: 게임 진입, 분위기 제시, BGM 재생 권한 확보

### 화면 요소
| 요소 | 설명 |
|---|---|
| 게임 타이틀 | "아무도 믿지마" 로고 텍스트 또는 이미지 |
| 배경 | 폐교 외관 또는 어두운 단색 배경 (AI 생성 이미지 1장) |
| [시작하기] 버튼 | 클릭 시 P-01 인트로로 전환 |
| BGM 토글 | ON/OFF (기본 ON) |

### 기능 상세
1. **시작하기 클릭** → 게임 상태 초기화(하트 3, 열쇠조각 0, 판별 도구 2) → P-01 전환
2. **BGM 자동재생 정책 대응**: 브라우저는 사용자 인터랙션 전 오디오 자동재생을 차단하므로, BGM은 반드시 [시작하기] 클릭 시점에 `play()` 호출 (타이틀 화면 진입 즉시 재생 시도 금지)
3. 페이드 인 연출(선택): 타이틀 텍스트 서서히 등장

### 예외 처리
- 이미지 로드 실패 시 단색 배경 + 텍스트만으로 표시 (게임 진행 차단 없음)

---

## 2. P-01 인트로 (입장 연출)

**목적**: 세계관 도입, "문이 잠겼다"는 폐쇄 상황 확립, 판별 도구 튜토리얼

### 화면 요소
| 요소 | 설명 |
|---|---|
| 정지 이미지 | 잠긴 문 / 폐교 입구 |
| 내레이션 텍스트 | 입장 상황 서술 (타이핑 효과 선택) |
| 튜토리얼 텍스트 | 하트/열쇠조각/판별 도구 규칙 + 판별 결과 기호(`.....` / `.\|.\|.\|.`) 의미 안내 1~2줄 |
| [계속] 버튼 또는 화면 클릭 | 다음 텍스트 진행 → 완료 시 스테이지 1 진입 |
| [대화 건너뛰기] 버튼 | 이미지 우측 상단. 인트로 대사를 건너뛰고 튜토리얼/진입 버튼 표시 |

### 기능 상세
1. 텍스트를 문단 단위로 순차 출력, 클릭 시 다음 문단
2. **판별 도구 기호 설명을 이 화면에서 반드시 노출** (기획서 4항 요구사항 — 첫 사용 시 로딩 애니메이션으로 오인 방지)
3. 마지막 문단 이후 스테이지 1(P-10, stageId=1)로 전환, BGM 크로스페이드
4. [대화 건너뛰기]는 대사만 생략하며 탐지기 기호 안내는 유지

### 예외 처리
- 타이핑 효과 중 클릭 시: 해당 문단 전체 즉시 출력 (스킵)

---

## 3. P-10 스테이지 화면 (스테이지 1~5 공통 템플릿)

**목적**: 핵심 게임 루프 수행 — 서술문 확인 → 목소리 대사 청취 → (판별 도구) → 선택 → 판정

### 3-1. 화면 요소 (레이아웃)

| 영역 | 요소 | 설명 |
|---|---|---|
| 상단 HUD | 하트 표시 | ♥ 아이콘 × 현재 개수 (0~3) |
| 상단 HUD | 열쇠조각 표시 | 🗝 아이콘 × 현재 개수 (0~5) |
| 상단 HUD | 판별 도구 잔여 | 도구 아이콘 × 잔여 횟수 (0~2) |
| 중앙 | 스테이지 이미지 | AI 생성 정지 이미지 1~2장 (복도/교실/화장실/보건실/복도') |
| 하단 텍스트 박스 | 서술문(내레이션) | 스테이지 상황 서술. **향후 모순 판별의 근거가 되는 사실 정보 포함** |
| 하단 텍스트 박스 | 목소리 대사 | 서술문 이후 출력. 진실/거짓 버전 중 해당 스테이지 플래그에 따른 텍스트 |
| 하단 텍스트 박스 | 판별 결과 출력 | 도구 사용 시 동일 영역에 `.....` 또는 `.\|.\|.\|.` 렌더링 |
| 이미지 우측 상단 | [대화 건너뛰기] 버튼 | 서술문과 목소리 대사를 생략하고 조사 단계로 이동 |
| 이미지 핫스팟 | 조사 오브젝트 3개 | 모두 조사해야 기억 침식과 선택 단계가 순서대로 열림 |
| 우측 패널 | 증거 수첩 | 확인한 단서 이름과 사실만 누적. 별도 추론/정답 해설은 표시하지 않음 |
| 선택 영역 | 선택지 A / B / C 버튼 | 조사 완료 및 기억 침식 확인 후 활성화 |
| 선택 영역 | [판별 도구 사용] 버튼 | 조건부 활성화 (아래 참조) |

### 3-2. 진행 플로우

```
스테이지 진입
 → 이미지 + 서술문 출력 (클릭으로 문단 진행)
 → 목소리 대사 출력
 → 오브젝트 3개 조사
 → 무작위 기억 침식 1회 표시
 → 선택지 A/B/C + 판별 도구 버튼 활성화
    ├─ (선택) 판별 도구 사용 → 도구 -1 → 결과 기호 출력 → 정답 선택지 초록 강조
    └─ 선택지 클릭 → 정오 판정
         ├─ 옳은 선택 → 열쇠조각 +1 → 결과 연출 → 다음 스테이지 전환
         └─ 틀린 선택 → 하트 -1
              ├─ 하트 0 → P-20 리셋 화면
              └─ 하트 ≥ 1 → 열쇠조각 없이 다음 스테이지 전환
```

### 3-3. 기능 상세

**F-10-1 텍스트 출력**
- 서술문 → 목소리 대사 순으로 출력. 화자 구분 스타일 필수 (예: 서술문은 일반체, 목소리 대사는 따옴표+색상/서체 차이)
- 목소리 대사는 스테이지별 후보 8개 중 1개를 진입 시 랜덤 선택. 다른 장소의 핵심 소재가 섞이지 않도록 장소별로 분류
- 선택지는 스테이지별 후보 6개(정답 2개 이상, 오답 3개 이상)에서 정답 1개와 오답 2개를 추첨한 뒤 A/B/C 위치를 무작위로 섞음
- 이미지 우측 상단 [대화 건너뛰기]를 누르면 서술문·목소리를 함께 생략하고 조사 단계로 이동

**F-10-2 판별 도구 사용**
- 활성화 조건: `판별 도구 잔여 ≥ 1` AND `하트 ≥ 1`
- 사용 시: 도구 잔여 -1. 하트는 소모하지 않음
- 결과 렌더링: 현재 목소리 대사의 진위 플래그 참조
  - 진실 → `.....` (일정 간격으로 점이 하나씩 출력되는 담담한 애니메이션)
  - 거짓 → `.|.|.|.` (불규칙 간격/흔들림 애니메이션)
- 결과 표시 후 선택지 다시 활성화하고 정답 선택지에 초록색 테두리를 표시. 같은 스테이지에서 도구 재사용 불가(대사당 1회)

**F-10-3 선택 판정**
- 각 스테이지 JSON의 선택지 후보마다 `isCorrect` 정의
- 옳은 선택: 전체 화면 경고 효과 없이 열쇠조각 +1 → 짧은 성공 서술문 → 전환
- 틀린 선택: 붉은 경고/노이즈 효과 → 하트 -1 → 짧은 실패 서술문 → 하트 0이면 P-20, 아니면 전환
- 판정 직후 모든 버튼 비활성화 (더블클릭 중복 판정 방지)

**F-10-4 스테이지 전환**
- 화면 페이드 아웃 → 다음 스테이지 데이터 로드 → 페이드 인
- BGM: 스테이지 전환 시 페이드 아웃/인 (스테이지별 트랙 또는 공통 트랙 — 데이터에 트랙 ID 정의)
- 스테이지 5 진입 시: 스테이지 1과 동일 배경(복도)이되 차이 요소 적용 여부는 미결(기획서 8항) — 이미지 파일만 교체하면 되도록 `imageUrl`을 데이터로 분리해 둘 것

**F-10-5 엔딩 분기 (스테이지 5 선택 완료 시)**
- 스테이지 5의 선택/판정 처리 후:
  - `열쇠조각 = 5` → P-30 진엔딩
  - `열쇠조각 < 5` → P-31 배드엔딩
  - 단, 스테이지 5에서 틀린 선택으로 하트 0이 되는 경우 → P-20 리셋 (엔딩 분기보다 우선)

### 3-4. 상태 정의

```typescript
interface GameState {
  gamePhase: 'title' | 'intro' | 'stage' | 'reset' | 'endingTrue' | 'endingBad';
  stageId: 1 | 2 | 3 | 4 | 5;
  hearts: number;        // 0~3, 시작 3
  keyFragments: number;  // 0~5, 시작 0
  detectorUses: number;  // 0~2, 시작 2
  currentVoiceLineId: string;   // 이번 스테이지에서 랜덤 선택된 대사 ID
  currentChoiceIds: string[];   // 정답 1 + 오답 2를 무작위 순서로 선택한 ID
  currentIntrusionText: string; // 이번 스테이지에서 선택된 기억 침식 문구
  detectorUsedThisStage: boolean;
  textCursor: number;    // 현재 출력 중인 문단 인덱스
}
```

### 3-5. 스토리 데이터(JSON) 스키마

```json
{
  "stages": [
    {
      "id": 1,
      "name": "복도",
      "imageUrl": "/images/stage1_hallway.png",
      "bgmTrack": "main",
      "narration": ["서술문 문단1", "서술문 문단2"],
      "intrusionTexts": ["기억 침식 변형1", "기억 침식 변형2", "기억 침식 변형3"],
      "voiceLines": [
        { "id": "s1-v1", "text": "대사", "isLie": false, "contradictsStage": null },
        { "id": "s1-v2", "text": "대사 변형", "isLie": false, "contradictsStage": null }
      ],
      "choices": [
        { "id": "A", "text": "정답 후보1", "isCorrect": true },
        { "id": "B", "text": "오답 후보1", "isCorrect": false },
        { "id": "C", "text": "오답 후보2", "isCorrect": false },
        { "id": "D", "text": "정답 후보2", "isCorrect": true },
        { "id": "E", "text": "오답 후보3", "isCorrect": false },
        { "id": "F", "text": "오답 후보4", "isCorrect": false }
      ],
      "resultText": { "correct": "성공 서술(선택)", "wrong": "실패 서술(선택)" }
    }
  ]
}
```

- `isLie`: 판별 도구 결과 렌더링에 사용
- `contradictsStage`: 이 거짓 대사가 어느 스테이지 서술문과 모순되는지 참조 (콘텐츠 검수용 메타데이터, 복선-회수 간격 1~2스테이지 검증에 활용)
- `intrusionTexts`: 스테이지별 2개 이상 필요. 현재 데이터는 3개이며 정답을 직접 설명하지 않는 맥거핀으로 운용
- `choices`: ID는 데이터 식별용이며 화면의 A/B/C 표시는 무작위 배열 인덱스로 결정

### 3-6. 조사·기억 침식·증거 수첩

1. 스테이지마다 오브젝트 3개를 모두 조사해야 선택 단계로 진행 가능
2. 단서를 연속으로 누른 뒤 한 번 닫거나, 단서마다 [조사 마치기]를 눌러도 동일하게 완료 처리
3. 마지막 조사 창을 닫으면 해당 스테이지에서 선택된 기억 침식을 한 번 표시
4. 기억 침식을 닫은 뒤에만 선택 패널 표시
5. 증거 수첩에는 오브젝트 이름과 직접 확인한 `clue`만 기록하고 `deduction` 해설은 노출하지 않음

---

## 4. P-20 리셋(게임오버) 화면

**목적**: 하트 소진 시 실패 통보 및 재시작

### 화면 요소 및 기능
| 요소 | 설명 |
|---|---|
| 실패 연출 텍스트/이미지 | 짧은 암전 + 실패 문구 (배드엔딩과 구분되는 가벼운 연출) |
| [처음부터] 버튼 | 전체 상태 초기화 → 스테이지 1 재시작 (타이틀 경유 여부는 선택) |

- 리셋 시 하트 3 / 열쇠조각 0 / 판별 도구 2로 완전 초기화
- 재시작 시 목소리 대사 랜덤 재추첨 → 반복 플레이에도 텍스트 변화

---

## 5. P-30 진엔딩 / P-31 배드엔딩 화면

**목적**: 서사 마무리 및 회차 종료

### 화면 요소
| 요소 | 진엔딩 (P-30) | 배드엔딩 (P-31) |
|---|---|---|
| 이미지 | 폐교 탈출 이미지 | 누군가에게 끌려가는 이미지 |
| 엔딩 텍스트 | 목소리 정체 공개 + 탈출 서사 | 속아서 감금되는 서사 |
| BGM | 엔딩용 트랙 또는 페이드 아웃 | 동일 |
| [다시 하기] 버튼 | 타이틀(P-00)로 복귀 | 동일 |
| 엔딩 표기(선택) | "TRUE END" | "BAD END" — 다른 엔딩 존재 암시로 재플레이 유도 |

### 기능 상세
1. 텍스트 문단 순차 출력 (클릭 진행), 완료 후 [다시 하기] 노출
2. 다시 하기 → 상태 완전 초기화 → P-00
3. (선택) 배드엔딩에 "열쇠조각이 부족했다" 힌트 문구 → 진엔딩 조건을 간접 학습

---

## 6. 공통 기능 명세

| ID | 기능 | 설명 |
|---|---|---|
| C-1 | 상태 관리 | 단일 GameState로 관리. 새로고침 시 초기화 허용(5~8분 게임이므로 저장 불필요) — localStorage 사용 안 함 |
| C-2 | BGM 컨트롤 | 전 화면 공통 토글, 스테이지 전환 시 페이드, 자동재생 정책 대응(첫 인터랙션 후 재생) |
| C-3 | 효과음 | 로컬 SFX 사용. 클릭·시작·이동·탐지·정답·오답을 지원하고 전역 ON/OFF·볼륨 조절 제공 |
| C-4 | 이미지 프리로드 | 다음 스테이지 이미지를 현재 스테이지 진행 중 미리 로드 (전환 시 깜빡임 방지) |
| C-5 | 텍스트 스킵 | 출력 중 클릭 = 현재 문장 즉시 표시. 인트로/스테이지 이미지 우측 상단 버튼 = 해당 구간 대화 전체 건너뛰기 |
| C-6 | 반응형 | 모바일 세로 화면 기준 우선 (이미지 상단 + 텍스트 하단), 데스크톱은 중앙 정렬 |
| C-7 | 입력 잠금 | 판정/전환 애니메이션 중 모든 인터랙션 비활성화 |

---

## 7. 미결 사항 (기획서 8항 연동)

| # | 항목 | 명세서 반영 위치 |
|---|---|---|
| 1 | 판별 도구 첫 등장 설명 문구 | P-01 튜토리얼 텍스트로 배치 확정, 문구만 미정 |
| 2 | 스테이지별 서술문/대사/선택지 실제 문구 | JSON 스키마 확정, 콘텐츠 채우기 필요 |
| 3 | 엔딩 텍스트 및 이미지 프롬프트 | P-30/P-31 요소 정의 완료, 콘텐츠 미정 |
| 4 | 스테이지 5 복도 차별화 여부 | `imageUrl` 데이터 분리로 양쪽 대응 가능하게 설계 |
| 5 | 하트 1개 시 판별 도구 처리 | 하트 비용을 제거하고 사용 허용으로 확정 |
