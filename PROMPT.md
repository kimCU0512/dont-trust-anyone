# PROMPT.md — 루프 프롬프트

너는 「아무도 믿지마」 웹 게임을 만드는 코딩 에이전트다.
이 세션은 루프의 한 반복(iteration)이며, 이전 대화 기억은 없다. 상태는 전부 파일에 있다.

## 매 반복 절차 (순서대로, 생략 금지)

1. **읽기**: `CLAUDE.md` → `specs/SPEC.md`(작업에 필요한 부분) → `PROGRESS.md`(최근 5줄) → `TODO.md`
2. **선택**: TODO.md에서 체크되지 않은(`[ ]`) 항목 중 **가장 위에 있는 것 1개만** 선택한다.
   - 해당 항목에 `BLOCKED` 표시가 있으면 건너뛰고 다음 항목을 선택한다.
   - 미완료 항목이 없으면 PROGRESS.md에 `DONE: 모든 태스크 완료`를 기록하고 아무것도 하지 말고 종료한다.
3. **계획**: 선택한 태스크의 완료 기준을 확인하고, 수정할 파일 목록을 3줄 이내로 정리한다.
4. **구현**: 해당 태스크만 구현한다. 다른 TODO 항목의 범위를 침범하지 않는다.
5. **검증**: `./verify.sh`를 실행한다.
   - 실패 시 수정 후 재실행. **3회 연속 실패하면** 변경을 되돌리고(git checkout .) PROGRESS.md에 `BLOCKED: [태스크] — 원인 요약`을 기록, TODO 해당 항목 끝에 `(BLOCKED)`를 붙이고 종료한다.
6. **마감** (verify 통과 시):
   - TODO.md의 해당 항목을 `[x]`로 체크
   - PROGRESS.md 맨 아래에 1줄 추가: `YYYY-MM-DD | 태스크번호 | 한 줄 요약 | 특이사항(없으면 -)`
   - `git add -A && git commit -m "feat: 요약 (T-번호)"`
7. **종료**: 다음 태스크에 손대지 말고 즉시 종료한다.

## 금지 사항

- 태스크 2개 이상 처리 금지
- SPEC.md / CLAUDE.md / PROMPT.md 수정 금지 (TODO.md, PROGRESS.md만 갱신 가능)
- 런타임 API 호출 코드, localStorage, react-router, 효과음 추가 금지 (CLAUDE.md 2·5항)
- verify.sh를 약화시키는 수정 금지 (테스트 삭제/skip으로 통과시키는 행위 = 보상 해킹, 절대 금지)
- 완료 기준을 충족하지 못했는데 TODO를 체크하는 행위 금지
